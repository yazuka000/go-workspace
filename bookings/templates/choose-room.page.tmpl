{{template "base" .}}

{{define "content"}}
<div class="container">
  <div class="row">
    <div class="col">
      <h1>Choose a room</h1>

      {{range $room := index .Data "rooms"}}
        {{.RoomName}}<br />
      {{ end }}
    </div>
  </div>
</div>
{{ end }}
